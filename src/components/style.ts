import type { SxProps } from '@mui/material';

interface componentStyleProps {
  [key: string]: SxProps;
}

export const componentStyle: componentStyleProps = {
 tableHeaderSx:{
  background: '#d2d2d2',
 }
}